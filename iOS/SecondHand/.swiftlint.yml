disabled_rules:
- line_length
- trailing_whitespace
- file_length
- force_try

opt_in_rules:
- force_unwrapping
- function_body_length

excluded:
- Pods

force_unwrapping:
  severity: error
  
function_body_length:
  - 25 # warning
  - 40 # error
